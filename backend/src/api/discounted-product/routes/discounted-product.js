'use strict';

/**
 * discounted-product router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::discounted-product.discounted-product');
